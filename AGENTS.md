# このリポジトリで作業する人・道具へ

ゲーム制作サークル **ゾウノアシゲームズ** の公式サイト（https://www.zounoashi.com）です。
GitHub Pages + 独自ドメインで公開しています。ビルド工程はありません。

ゲームを1本足す手順そのものは **[games/README.md](games/README.md)** にあります。
ここには、それ以前の「このリポジトリ全体の決めごと」を書いています。

---

## 決めごと（ここだけは外さない）

### 1. 細かい変更は確認なしで push、大きな判断は先に確認

頼まれた細かい変更は、必要な動作確認を済ませて **commit と push まで進める**。
保存先の変更、軽微な不具合修正、共有カードの調整、雛形や手順書の更新などは、
その都度の確認や追加の「pushして」を求めなくてよい。

**新しいゲームを公開するかどうか、公開済みゲームの取り下げ、大幅な内容変更など、
大きな判断だけは持ち主に確認する**。公開の承認がまだないゲームは下書きのままにする。
すでにその判断について明確な指示を受けている場合は、重ねて確認しない。
push 前には含まれる変更を確認し、未承認の新規公開などを一緒に公開しないこと。
作業を終えるときは、push したか、手元のコミットで止まっているかをはっきり伝える。

### 2. 頼まれていない文字を出さない

ゲーム画面にもサイトにも、**頼まれた文字だけ**を出します。
遊びかたの説明、煽り文句、見出し、ラベル、「〜しよう！」のたぐいを気を利かせて足さない。
持ち主が自分で決めたいところです。

### 3. 日本語で書く

コメントも、コミットメッセージも、画面に出る文字も日本語。
横文字をそのまま使わず、ふつうの言葉にします（「ハーネス」→「台」、
「アセット」→「素材」、「デバッグ用フック」→「覗き穴」のように）。

### 4. ブランチは `main` ひとつ

機能ブランチは作りません。`main` に直接コミットします。
前の作業の名残で別のブランチに居ることがあるので、**始める前に `git branch` を見る**こと。

### 5. 消すもの・上書きするものは先に見る

素材の差し替え、公開済みファイルの書き換えは、中身を確かめてから。

---

## 置き場所と構成

| | |
|---|---|
| 実体 | `C:\Users\megus\Documents\zounoashi`（ここ） |
| remote | `git@github.com:TomozoP/zounoashi.git` / ブランチ `main` |
| 公開 | `main` に push すると GitHub Pages が自動で反映 |
| ドメイン | ムームードメイン + ムームーDNS。`CNAME` ファイルは GitHub が管理するので触らない |

```
index.html        サイト全部（自作の軽いハッシュルーティング。#/about のような形）
img/              ロゴ・アイコン
counter.gs        プレイ数カウンター（Google Apps Script。置きかたはファイル冒頭に）
games/
  README.md       ゲームを足す手順
  share.js        Xへのシェア（zShare）
  pad.js          ジョイパッドをキーに化けさせる共通の受け口
  _local.js       下書きの一覧。localhost のときだけ読まれる
  _template/      雛形
  _tools/         道具（下記）
  <id>/           公開済みのゲーム（HTML1枚 + img/）
  _<id>/          まだ公開しないゲーム
```

### `_` で始まる名前は公開されない

制作中の確認に毎回使う固定リンクは `https://www.zounoashi.com/preview/`。
実体は `preview/index.html`。一覧には載せず、制作するゲームに合わせて中身を差し替えます。
今はロゴが回転する待機ページです。正式公開するときはゲーム固有の場所と正式リンクを使い、
`/preview/` は次の制作でも同じURLのまま使います。
`preview/auto-reload.js` が表示中は30秒ごと、画面へ戻ったとき・通信復帰時にも更新を確認します。
制作中の内容を更新するcommitでは、`preview/index.html` の `preview-version` を必ず新しい値にします。
絵・音・別ファイルだけを直す場合も同じです。読み込む素材やスクリプトのURLにも同じ版の `?v=` を付けます。
中身を差し替えても版の指定と自動更新スクリプトの読み込みを残してください。

GitHub Pages（Jekyll）は `_` で始まるファイル・フォルダを配信しません。
これを使って、**下書きは `games/_<id>/` に置いたまま push できる**ようにしています。
道具やテスト（`_tools/`）も公開されません。

サイトの一覧に載せるゲームは `index.html` の `GAMES` 配列に直接書きます。
一覧に載せない制作中の公開は `node games/_tools/publish.js <id> --unlisted`。
本体は `games/<id>/`、登録は `games/_local.js` に残し、直接URLで開きます。
完成後は通常の `publish.js <id>` で一覧と正式な共有ページへ切り替えます。
くわしくは `games/README.md` の「制作中の隠しリンク」を参照してください。
下書きのうちは `games/_local.js` に書いておくと、**localhost で見たときだけ**カードが出ます。

---

## ゲームを足す

くわしくは [games/README.md](games/README.md)。いちばん短い形は：

```bash
node games/_tools/new.js hoge "ほげゲーム" "ひとこと説明"   # 雛形を写す
# index.html の ▼UPDATE ▼DRAW ▼SHARE を書く
node games/_tools/img.js 素材/ -o games/_hoge/img -w 1080  # 絵を webp に
node games/_tools/smoke.js games/_hoge/index.html          # 壊れていないか
node games/_tools/thumb.js hoge                            # サムネ
node games/_tools/publish.js hoge                          # 公開の形にする
git add -A && git commit                                   # 新しいゲームの公開は承認後に push
```

揃えるかたち（タテ画面・横540固定、`full: true`、押しどころはゲーム座標で63以上離す、
`window.__probe` を残す、など）は games/README.md の表にまとめてあります。

---

## 動く確認のしかた

**ブラウザのプレビューでゲームは遊べません。** ペインが表に出ていない間
`requestAnimationFrame` が止まるので、3秒待っても2〜3コマしか進みません。
「動かしてみたけど何も起きない」はたいていこれです。

代わりに **偽のDOMの上で自動運転** します。台は `games/_tools/harness.js`。

```js
var load = require("./games/_tools/harness");
var g = load("games/_hoge/index.html");
g.step(60);                 // 1秒ぶん進める
g.tap(270, 400);            // ゲーム座標のまま触る
g.press(" ");               // スペース
g.pad({ press: true });     // ジョイパッド
g.view(390, 844);           // 画面の形を変える
g.probe.now();              // ゲーム側の覗き穴
g.until(function () { return g.probe.now().state === "result"; });
```

- 見た目だけなら、開始直後の1コマをスクリーンショットすれば足ります
- `smoke.js` は「落ちないか」しか見ません。**遊びとして成立するか**は
  ゲームごとにテストを書きます。`games/_tools/tests/wanko-*.js` が見本です
- 難しさは**数字で測る**こと（導火線の秒数、必要な連打の速さ、判定の通過ミリ秒、
  押しどころ同士の距離）。感覚で決めない

---

## 道具（`games/_tools/`）

| | |
|---|---|
| `new.js` | 雛形を写して、手元の一覧に登録する |
| `img.js` | png/jpg を webp にする。縮小もできる |
| `smoke.js` | 落ちないかの共通確認 |
| `harness.js` | 偽DOMの台。ゲームごとのテストはこれで書く |
| `thumb.js` | 動いている画面からサムネ（600x600 webp）を作る |
| `publish.js` | `_` を外して `GAMES` に入れる（commit と push はしない） |
| `tests/` | ゲームごとのテスト |

### 要るもの

- **Node**（v22 で動かしています）。npm の追加は入れていません。素のままで動きます
- **Edge か Chrome**。`img.js` と `thumb.js` が、画面なしで裏から借ります
  （Node だけでは webp を作れないため）。探す場所は各ファイルの `browser()` にあります

### ローカルで見る

`index.html` をそのまま開くのではなく、**HTTPで配信して**見ます（`file://` だと
一覧の読み込みなどが動きません）。ポートは何でも構いません。

```bash
npx --yes http-server . -p 8735 -c-1
```

`localhost` のときだけ、下書き一覧が読まれ、プレイ数は数えられず、
ゲームのURLに `?v=` が付きます（古いものが残らないように）。

---

## よくある詰まり

| | |
|---|---|
| プレビューでゲームが動かない | 仕様です。上の「動く確認のしかた」を見ること |
| 直したのに変わらない | 手元は `?v=` が自動で付きます。それでも古いときは Ctrl+Shift+R |
| 下書きが一覧に出ない | `games/_local.js` に行があるか、`localhost` で見ているか |
| 押しどころが近すぎる | スマホ（横375px）で44px以上 ＝ **ゲーム座標で63以上**。面板を広げる・2段にする |
| キーが効かない | 枠の外をクリックするとフォーカスが外れます。タップでも遊べるようにしておく |
| ジョイパッドが効かない | `<script src="../pad.js"></script>` を読んでいるか（雛形には入っています） |

### ゲームの開始とスマホ確認

新作はテンプレートの「START」待機画面から始め、ゲームのページ内のどこをタップしても、
指を離した時点で開始できるようにする。開始タップをゲーム本編の操作に混ぜない。
音のあるゲームはその開始操作で音声を有効にする。長押しの選択・メニューも抑える。
具体的な作りと録画・実機検証の注意点は games/README.md の
「開始画面・スマホの音・長押し」「録画で今回直した点」を参照する。
PCの自動確認だけで、iPhone実機の音声不具合が直ったと断定しない。
