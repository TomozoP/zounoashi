# 音声の処理

音量と候補の照合は端末内で処理する。単語の認識はブラウザが processLocally に対応し、日本語の端末内モデルが利用可能と確認できた場合だけ開始する。外部認識への切替はしない。非対応時は、2.4秒ごとに光る候補を声量で選ぶ。これは言葉の認識ではない。

実装の参照：https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition/processLocally
